export type Weather = {
  temp?: number,
  description?: string,
  main?: string,
  pressure?: number,
  humidity?: number,
}

export type User = {
  token?: string,
  profileUrl?: string,
  name?: string,
}
